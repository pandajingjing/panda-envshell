{{INSTALL_DIR_LOG_BIN}}/*.log {
    daily
    rotate 10
    create 0664 {{ENV_USER}} {{ENV_GROUP}}
    missingok
    notifempty
    compress
    delaycompress
    postrotate
        {{INSTALL_DIR_BIN_BIN}}/sbin/squid -k restart
    endscript
    sharedscripts
}