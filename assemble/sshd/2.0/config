BIN_PORT=22

BIN_REMOTE_TUNNEL=yes

BIN_REMOTE_TUNNEL_LOCAL_PORT=22
BIN_REMOTE_TUNNEL_REMOTE_PORT=22
BIN_REMOTE_TUNNEL_REMOTE_USER=$DEPLOY_USER
BIN_REMOTE_TUNNEL_REMOTE_HOST=xx.xx.xx.xx

INSTALL_DIR_LOG_BIN=$INSTALL_DIR_LOG'/sshd'

BIN_CONFIGURE_VARS='BIN_PORT INSTALL_DIR_LOG_BIN BIN_REMOTE_TUNNEL BIN_REMOTE_TUNNEL_LOCAL_PORT BIN_REMOTE_TUNNEL_REMOTE_PORT BIN_REMOTE_TUNNEL_REMOTE_USER BIN_REMOTE_TUNNEL_REMOTE_HOST'
