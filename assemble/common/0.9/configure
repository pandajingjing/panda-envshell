#!/bin/bash

#do some configure script

showInfo 'configure common start.'
    configBin $sExecBinVersionAssembleDir'/conf/profile' '/etc/profile'  '#custom language start from here:' '#custom language end here.'
showInfo 'configure common successfully.'