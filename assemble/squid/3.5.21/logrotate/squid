{{INSTALL_DIR_LOG_BIN}}/*.log {
    compress
    delaycompress
    notifempty
    daily
    missingok
    sharedscripts
    postrotate
        {{INSTALL_DIR_BIN_BIN}}/sbin/squid -k restart
    endscript
}
