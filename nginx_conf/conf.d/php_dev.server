server {
    listen 40083;
    server_name *.dev.jxu.life *.dev.pandajingjing.net;
    root /data/app/index;

    set $phpfile web_dev.php;

    if ($host ~ ^api.*\.dev\.jxu\.life$){
        set $phpfile api_dev.php;
    }

#    if ($host ~ ^m\..*\.dev\.jxu\.life$){
#        set $phpfile mobile_dev.php;
#    }

    if ($host ~ ^api.*\.dev\.pandajingjing\.net$){
        set $phpfile api_dev.php;
    }

#    if ($host ~ ^m\..*\.dev\.pandajingjing\.net$){
#        set $phpfile mobile_dev.php;
#    }

    index $phpfile;

    location / {
        try_files $uri $uri/ /$phpfile?$args;
    }

    location ~ \.php$ {
        fastcgi_pass   php-fpm;
        fastcgi_index  $phpfile;
        fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
        include        fastcgi_params;
        fastcgi_keep_conn on;
    }

    access_log /data/logs/nginx/php.dev.server.access.log more;
    error_log /data/logs/nginx/php.dev.server.error.log;
}
